notes/
__pycache__/
.env
.env.*

# Generated model outputs are tracked so the public Pages deploy can
# serve the GHOST and ORACLE views. Regenerated by train_sleeper_model.py
# / train_oracle_model.py respectively; commit the refreshed JSON
# alongside any data refresh.

# CFBD pipeline artifacts (key + cache + raw CSV are local-only;
# college_stats.csv is derivable from fetch_college_stats.py)
data/.cfbd_api_key
data/college_cache/
data/college_fetch.log
data/college_stats.csv
data/combine_data.csv

# Overnight chain artifacts
data/overnight_chain.sh
data/overnight_summary.log
